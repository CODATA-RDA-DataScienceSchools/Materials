library(tidyverse)
data("population")
View(population)
summary(population)
summary(as.factor(country))
summary(as.factor(population$country))
ggplot(population,
aes(x = country, y = population)) +
geom_boxplot()
?subset
?starts_with
ggplot(filter(population$country, starts_with("A")),
aes(x = country, y = population)) +
geom_boxplot()
ggplot(select(population$country, starts_with("A")),
aes(x = country, y = population)) +
geom_boxplot()
ggplot(subset(population, filter(country, starts_with("A")),
geom_boxplot()
ggplot(subset(population, filter(country, starts_with("A"))),
aes(x = country, y = population)) +
geom_boxplot()
subset(population, filter(country, starts_with("A"))
population %>%
scan_data()
library(pointblank)
population %>%
scan_data()
ggplot(subset(population, starts_with("A")),
aes(x = country, y = population)) +
geom_boxplot()
geom_boxplot()
population %>%
filter(starts_with(country, "A"))
# Sample data frame
df <- data.frame(your_column = c("Apple", "Banana", "Apricot", "Grapes", "Orange"))
# Using starts_with() to filter data
filtered_data <- df %>%
filter(starts_with(your_column, "A"))
# Using starts_with() to filter data
filtered_data <- df %>%
filter(grepl(your_column, "A"))
?grepl
# Using starts_with() to filter data
filtered_data <- df %>%
filter(grepl("A"))
# Using starts_with() to filter data
filtered_data <- df %>%
filter(grepl(x = country, pattern = "^A"))
# Using starts_with() to filter data
filtered_data <- df %>%
filter(grepl(x = your_column, pattern = "^A"))
View(filtered_data)
population %>%
filter(grepl(x = country, pattern = "^A")) %>%
ggplot(aes(x = country, y = population)) +
geom_boxplot()
population %>%
filter(grepl(x = country, pattern = "^A")) %>%
ggplot(aes(x = country, y = population)) +
geom_boxplot() +
theme_bw()
View(population)
summary(population)
summary(as.factor(population$country))
population %>%
scan_data()
